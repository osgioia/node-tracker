# Database URL for the test environment
# The user, password, and database name can be whatever you want.
# The port (5433) is different from the default (5432) to avoid conflicts.
DATABASE_URL="postgresql://testuser:testpass@localhost:5433/testdb?schema=public"

# Redis URL for the test environment
# The port (6380) is different from the default (6379).
REDIS_URL="redis://localhost:6380"

# Use a different secret for testing
JWT_SECRET="a-very-secure-and-long-secret-for-testing-purposes"
JWT_EXPIRES_IN="15m"

# Other variables
NODE_ENV=test